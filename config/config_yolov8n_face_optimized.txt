[property]
gpu-id=0
#0=RGB, 1=BGR
model-color-format=0
onnx-file=../models/yolov8n_face/yolov8n-face.onnx
model-engine-file=../models/yolov8n_face/yolov8n-face.onnx_b4_gpu0_fp16.engine
labelfile-path=../models/yolov8n_face/labels.txt
# 1: Primary, 2: Secondary
process-mode=1
## 0=FP32, 1=INT8, 2=FP16 mode
network-mode=2
gie-unique-id=1
# 0: Detector, 1: Classifier, 2: Segmentation, 3: Instance Segmentation 100:Other
network-type=0
output-blob-names=boxes;scores;landmarks
## 0=Group Rectangles, 1=DBSCAN, 2=NMS, 3= DBSCAN+NMS Hybrid, 4 = None(No clustering)
cluster-mode=4
maintain-aspect-ratio=1
symmetric-padding=1
batch-size=4
num-detected-classes=1
output-tensor-meta=1

# custom detection parser
parse-bbox-func-name=NvDsInferParseYoloFace
custom-lib-path=../models/yolov8n_face/nvdsinfer_custom_impl_Yolo/libnvdsinfer_custom_impl_Yolo.so
net-scale-factor=0.0039215697906911373
force-implicit-batch-dim=0

# OPTIMIZED: Skip 2 frames for better throughput (3x speedup)
# Still maintains good detection quality due to temporal tracking
interval=3

enable-output-landmark=1

# OPTIMIZED: Detection filtering for better precision and performance
[class-attrs-all]
# Higher confidence threshold reduces false positives and processing load
pre-cluster-threshold=0.50
# Reduced top-K for better performance while keeping best detections
topk=12
# Stricter NMS for cleaner detection results
nms-iou-threshold=0.40
# Consistent post-processing threshold
post-cluster-threshold=0.50

# OPTIMIZED: Input object size filtering (reduce small/noisy detections)
input-object-min-width=30
input-object-min-height=30
input-object-max-width=800
input-object-max-height=800